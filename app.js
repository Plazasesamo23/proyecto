
let typed;
let escrito;
typed = new Typed('.typed',{
    strings:['Proyecto','1'],
    typeSpeed:100,
    startDelay:300,
    backSpeed:100,
    loop:true
    
    
});

escrito = new Typed('.escrito',{
    strings:['Pagina','de','inicio'],
    typeSpeed:100,
    startDelay:300,
    loop:true,
    backSpeed:100
});